name: universal_tv
description: "Cross-platform IPTV application."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  cached_network_image: ^3.4.1
  collection: ^1.19.1
  cupertino_icons: ^1.0.8
  dart_mappable: ^4.5.0
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  forui: ^0.15.1
  forui_assets: ^0.15.0
  go_router: ^16.2.0
  hydrated_bloc: ^10.1.1
  media_kit: ^1.2.0
  media_kit_libs_video: ^1.0.6
  media_kit_video: ^1.3.0
  microsoft_kiota_bundle: ^0.1.0
  muxa_xtream: ^0.1.2
  path_provider: ^2.1.5
  tiny_db: ^0.9.1+1
  uuid: ^4.5.1
  xml: ^6.5.0

dev_dependencies:
  build_runner: ^2.4.15
  dart_mappable_builder: ^4.5.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  # TODO: Set to false once we have custom video controls
  uses-material-design: true
  generate: true